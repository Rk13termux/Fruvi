name: fruvi
description: "Asistente de IA para frutas y verduras importadas + tienda profesional integrada."
version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1

  # Estado y arquitectura
  riverpod: ^2.5.1
  get_it: ^7.7.0

  # HTTP y APIs
  dio: ^5.7.0
  flutter_dotenv: ^5.1.0

  # Base de datos y offline
  supabase_flutter: ^2.5.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Generación de Excel
  excel: ^3.0.0
  path_provider: ^2.1.3
  open_file_plus: ^3.4.0

  # UI y animaciones
  flutter_staggered_animations: ^1.1.0
  lottie: ^3.1.0
  flutter_slidable: ^3.0.0

  # Otros
  url_launcher: ^6.3.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  #assets:
    #- assets/images/
    #- assets/icons/
    #- assets/animations/
    #- .env